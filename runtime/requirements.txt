bedrock-agentcore>=1.1.0
boto3>=1.40
# The DeepSeek Harness Python SDK is provided from the pinned monorepo checkout
# (PYTHONPATH=<DSH_REPO_ROOT>/python/sdk/src) — it depends only on pydantic.
pydantic>=2.12,<3
